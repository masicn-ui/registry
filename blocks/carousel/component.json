{
  "name": "carousel",
  "displayName": "Carousel",
  "description": "Horizontally scrollable carousel block with pagination indicators.",
  "category": "display",
  "version": "0.0.2",
  "files": [
    {
      "path": "Carousel.tsx",
      "target": "Carousel.tsx",
      "type": "component"
    },
    {
      "path": "useCarousel.ts",
      "target": "useCarousel.ts",
      "type": "utility"
    },
    {
      "path": "CarouselSlide.tsx",
      "target": "CarouselSlide.tsx",
      "type": "component"
    },
    {
      "path": "CarouselDots.tsx",
      "target": "CarouselDots.tsx",
      "type": "component"
    }
  ],
  "masicnVersion": "^0.0.1",
  "peerDependencies": {
    "react-native-reanimated": ">=3.0.0",
    "react-native-gesture-handler": ">=2.0.0",
    "react-native-worklets": ">=0.7.0"
  },
  "registryDependencies": [],
  "tags": [
    "carousel",
    "slider",
    "swipe"
  ],
  "hasTests": true,
  "hasAccessibility": true,
  "props": [
    {
      "name": "data",
      "type": "T[]",
      "required": true,
      "description": "Array of data items to render as carousel slides."
    },
    {
      "name": "renderItem",
      "type": "(item: T, index: number) => ReactNode",
      "required": true,
      "description": "Render function called for each slide item."
    },
    {
      "name": "keyExtractor",
      "type": "(item: T, index: number) => string",
      "required": false,
      "description": "Function to generate a unique key for each slide. Defaults to index-based key."
    },
    {
      "name": "animation",
      "type": "'scale' | 'fade' | 'parallax'",
      "default": "scale",
      "required": false,
      "description": "Transition animation style applied to slides as they scroll in/out."
    },
    {
      "name": "slideWidth",
      "type": "'full' | 'peek' | number",
      "default": "full",
      "required": false,
      "description": "Width of each slide. 'full' fills the screen, 'peek' shows adjacent slides (85% width), or a fixed pixel number."
    },
    {
      "name": "aspectRatio",
      "type": "number",
      "required": false,
      "description": "Width ÷ height ratio for each slide. Height is computed as Math.round(itemWidth / aspectRatio). Falls back to sizes.carouselSlideDefaultHeight when omitted."
    },
    {
      "name": "slideBorderRadius",
      "type": "keyof typeof radius",
      "default": "lg",
      "required": false,
      "description": "Border radius applied to each slide, using a radius design token key."
    },
    {
      "name": "gap",
      "type": "number",
      "required": false,
      "description": "Gap in pixels between slides. Only effective in peek/numeric width mode."
    },
    {
      "name": "showDots",
      "type": "boolean",
      "default": "true",
      "required": false,
      "description": "Whether to show pagination dot indicators below the carousel."
    },
    {
      "name": "dotVariant",
      "type": "'pill' | 'dot' | 'line'",
      "default": "pill",
      "required": false,
      "description": "Visual style of the pagination dot indicators."
    },
    {
      "name": "autoPlayInterval",
      "type": "number",
      "default": "0",
      "required": false,
      "description": "Milliseconds between auto-advancing slides. Set to 0 to disable auto-play."
    },
    {
      "name": "onItemPress",
      "type": "(item: T, index: number) => void",
      "required": false,
      "description": "Callback fired when a slide is tapped. Makes slides pressable when provided."
    },
    {
      "name": "testID",
      "type": "string",
      "required": false,
      "description": "Test identifier; each slide Pressable receives `{testID}-slide-{index}`."
    },
    {
      "name": "loop",
      "type": "boolean",
      "default": "true",
      "required": false,
      "description": "When true, wraps from the last slide back to the first (and vice versa) using clone-based infinite scroll. Requires at least 2 items."
    },
    {
      "name": "getLabel",
      "type": "(item: T, index: number) => string",
      "required": false,
      "description": "Optional function returning a label string for the active slide. When provided, the label is rendered below the dot indicator."
    }
  ],
  "examples": [
    {
      "label": "Example 1",
      "code": "getLabel={(item, index) => `${index + 1} of ${data.length} — ${item.title}`}"
    },
    {
      "label": "Example 2",
      "code": "// Full-width scale carousel with auto-play and loop\n<Carousel\n  data={slides}\n  renderItem={(item) => <Image source={{ uri: item.image }} style={{ flex: 1 }} />}\n  animation=\"scale\"\n  autoPlayInterval={3000}\n/>"
    },
    {
      "label": "Example 3",
      "code": "// Peek mode with parallax and a slide label\n<Carousel\n  data={cards}\n  renderItem={(card) => <Card {...card} />}\n  slideWidth=\"peek\"\n  animation=\"parallax\"\n  aspectRatio={4 / 3}\n  dotVariant=\"dot\"\n  getLabel={(item) => item.title}\n/>"
    }
  ]
}
