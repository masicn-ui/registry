{
  "name": "number-input",
  "displayName": "Number Input",
  "description": "Number input block with increment/decrement controls.",
  "category": "blocks",
  "version": "1.0.0",
  "files": [
    {
      "path": "NumberInput.tsx",
      "target": "NumberInput.tsx",
      "type": "component"
    }
  ],
  "masicnVersion": "^0.1.0",
  "peerDependencies": {},
  "registryDependencies": [
    "text-input",
    "button"
  ],
  "tags": [
    "number",
    "input",
    "stepper"
  ],
  "hasTests": false,
  "hasAccessibility": true,
  "props": [
    {
      "name": "value",
      "type": "number",
      "required": true,
      "description": "Current numeric value of the input."
    },
    {
      "name": "onValueChange",
      "type": "(value: number) => void",
      "required": true,
      "description": "Called with the new numeric value whenever it changes via stepping or direct keyboard entry."
    },
    {
      "name": "min",
      "type": "number",
      "required": false,
      "description": "Minimum allowed value. Defaults to negative infinity."
    },
    {
      "name": "max",
      "type": "number",
      "required": false,
      "description": "Maximum allowed value. Defaults to positive infinity."
    },
    {
      "name": "step",
      "type": "number",
      "default": "1",
      "required": false,
      "description": "Amount to increment or decrement the value when the +/- buttons are pressed."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "description": "Label text displayed above the input."
    },
    {
      "name": "helperText",
      "type": "string",
      "required": false,
      "description": "Helper text displayed below the input when there is no error."
    },
    {
      "name": "error",
      "type": "string",
      "required": false,
      "description": "Error message displayed below the input; also activates the error border colour."
    },
    {
      "name": "disabled",
      "type": "boolean",
      "default": "false",
      "required": false,
      "description": "Disables all interaction with the input and step buttons."
    },
    {
      "name": "containerStyle",
      "type": "ViewStyle",
      "required": false,
      "description": "Style applied to the outer container wrapping the label, input row, and helper text."
    },
    {
      "name": "testID",
      "type": "string",
      "required": false,
      "description": "Stable test identifier passed to the underlying TextInput for automated testing."
    }
  ]
}
