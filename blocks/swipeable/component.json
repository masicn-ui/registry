{
  "name": "swipeable",
  "displayName": "Swipeable",
  "description": "Swipeable row block with hidden actions revealed on swipe.",
  "category": "lists",
  "version": "0.0.2",
  "files": [
    {
      "path": "Swipeable.tsx",
      "target": "Swipeable.tsx",
      "type": "component"
    }
  ],
  "masicnVersion": "^0.0.1",
  "peerDependencies": {
    "react-native-gesture-handler": ">=2.0.0",
    "react-native-reanimated": ">=3.0.0"
  },
  "registryDependencies": [],
  "tags": [
    "swipe",
    "gesture",
    "row",
    "actions"
  ],
  "hasTests": true,
  "hasAccessibility": true,
  "props": [
    {
      "name": "children",
      "type": "ReactNode",
      "required": true,
      "description": "The primary content rendered on top of the swipeable row."
    },
    {
      "name": "leftActions",
      "type": "SwipeAction[]",
      "required": false,
      "description": "Actions revealed when the user swipes right. The first action is triggered when the swipe threshold is exceeded."
    },
    {
      "name": "rightActions",
      "type": "SwipeAction[]",
      "required": false,
      "description": "Actions revealed when the user swipes left. The first action is triggered when the swipe threshold is exceeded."
    },
    {
      "name": "containerStyle",
      "type": "ViewStyle",
      "required": false,
      "description": "Additional style applied to the outer container view."
    },
    {
      "name": "threshold",
      "type": "number",
      "default": "80",
      "required": false,
      "description": "Swipe distance in pixels required to trigger an action."
    },
    {
      "name": "size",
      "type": "'sm' | 'md' | 'lg'",
      "default": "md",
      "required": false,
      "description": "Controls the minimum row height and action button padding. 'sm' = 44px, 'md' = 64px, 'lg' = 80px."
    },
    {
      "name": "testID",
      "type": "string",
      "required": false,
      "description": "Test identifier forwarded to the outermost container View."
    }
  ],
  "examples": [
    {
      "label": "Example 1",
      "code": "<Swipeable\n  rightActions={[\n    {\n      label: 'Delete',\n      backgroundColor: 'error',\n      onPress: () => handleDelete(item.id),\n    },\n  ]}\n  leftActions={[\n    {\n      label: 'Archive',\n      backgroundColor: 'success',\n      onPress: () => handleArchive(item.id),\n    },\n  ]}\n>\n  <ListItem title={item.title} />\n</Swipeable>"
    },
    {
      "label": "Example 2",
      "code": "// Right-only delete action\n<Swipeable\n  rightActions={[\n    { label: 'Remove', backgroundColor: 'error', onPress: () => removeItem(item.id) },\n  ]}\n>\n  <CartRow item={item} />\n</Swipeable>"
    },
    {
      "label": "Example 3",
      "code": "// Multi-action right side with icons\n<Swipeable\n  rightActions={[\n    { label: 'Pin', icon: <PinIcon />, backgroundColor: 'info', onPress: () => pin(item.id) },\n    { label: 'Delete', icon: <TrashIcon />, backgroundColor: 'error', onPress: () => del(item.id) },\n  ]}\n>\n  <MessageRow message={item} />\n</Swipeable>"
    },
    {
      "label": "Example 4",
      "code": "// Compact size for a tighter list\n<Swipeable\n  size=\"sm\"\n  rightActions={[{ label: 'Done', backgroundColor: 'success', onPress: () => complete(item.id) }]}\n>\n  <TaskRow task={item} />\n</Swipeable>"
    }
  ]
}
