{
  "name": "drawer",
  "displayName": "Drawer",
  "description": "Persistent side navigation panel with temporary (overlay) and permanent (inline) variants. Swipe-to-dismiss, safe-area aware, and responsive.",
  "category": "navigation-ui",
  "version": "0.0.2",
  "files": [
    {
      "path": "Drawer.tsx",
      "target": "Drawer.tsx",
      "type": "component"
    }
  ],
  "masicnVersion": "^0.0.1",
  "peerDependencies": {
    "react-native-gesture-handler": ">=2.0.0",
    "react-native-reanimated": ">=3.0.0",
    "react-native-safe-area-context": ">=4.0.0"
  },
  "registryDependencies": [],
  "tags": [
    "navigation",
    "drawer",
    "side-nav",
    "menu"
  ],
  "hasTests": true,
  "hasAccessibility": true,
  "props": [
    {
      "name": "visible",
      "type": "boolean",
      "required": true,
      "description": "Whether the drawer is open. Ignored in permanent variant."
    },
    {
      "name": "onClose",
      "type": "() => void",
      "required": true,
      "description": "Called when the user dismisses the drawer. Only fires in temporary variant."
    },
    {
      "name": "children",
      "type": "React.ReactNode",
      "required": true,
      "description": "Navigation content rendered inside the drawer."
    },
    {
      "name": "variant",
      "type": "'temporary' | 'permanent'",
      "default": "temporary",
      "required": false,
      "description": "temporary = overlay with backdrop; permanent = always visible inline."
    },
    {
      "name": "side",
      "type": "'left' | 'right'",
      "default": "left",
      "required": false,
      "description": "Which edge the drawer slides from."
    },
    {
      "name": "width",
      "type": "number",
      "required": false,
      "description": "Drawer width. Fraction of screen width for temporary (default 0.75), fixed px for permanent (default 280)."
    },
    {
      "name": "showHandle",
      "type": "boolean",
      "default": false,
      "required": false,
      "description": "Show a drag handle indicator."
    },
    {
      "name": "hideBackdrop",
      "type": "boolean",
      "default": false,
      "required": false,
      "description": "Hide the semi-transparent backdrop in temporary mode."
    },
    {
      "name": "style",
      "type": "ViewStyle",
      "required": false,
      "description": "Additional style applied to the drawer surface."
    },
    {
      "name": "testID",
      "type": "string",
      "required": false,
      "description": "Test identifier for automated testing."
    }
  ],
  "examples": [
    {
      "label": "Example 1",
      "code": "// Temporary drawer on mobile\nconst [open, setOpen] = React.useState(false);\n<Drawer visible={open} onClose={() => setOpen(false)}>\n  <NavMenu />\n</Drawer>"
    },
    {
      "label": "Example 2",
      "code": "// Permanent drawer on tablet\n<Drawer variant=\"permanent\" visible onClose={() => {}}>\n  <NavMenu />\n</Drawer>"
    },
    {
      "label": "Example 3",
      "code": "// Imperative usage\nconst drawerRef = useRef<DrawerRef>(null);\n<Drawer ref={drawerRef} onClose={() => {}}>\n  <NavMenu />\n</Drawer>"
    },
    {
      "label": "Example 4",
      "code": "// Right-side drawer without backdrop\nconst [open, setOpen] = React.useState(false);\n<Drawer visible={open} onClose={() => setOpen(false)} side=\"right\" hideBackdrop>\n  <FilterPanel />\n</Drawer>"
    }
  ]
}
